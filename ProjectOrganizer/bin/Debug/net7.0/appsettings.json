// configuration setting - keys and values used in project

{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "AllowedHosts": "*",

  // Connect with database - server name, database name
  // Use MSSQL DB, Dapper library
    // Package Manager Console -> install dapper package, sql client (sqlconnection)
    // for this, create Utilities folder, have ConnectionString.cs
  "ConnectionStrings": {
    "DefaultConnection": "Server=CHERYL-PC; Database=MyProjectsOrganizer_db; Integrated Security=true;"
  }
}
